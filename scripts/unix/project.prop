#change project code names
project.shortform		= uglyforms
project.shortform.small = uglyforms
project.codename		= uglyforms

# Version
build.version 	= 2.0.0
#build.number	= Will be created dynamically
rb.build.version	= 1.1.0
rb.name		= RBSL.

# Output settings
file.package		= ${project.shortform}
file.compression	= gzip
file.extension		= zip

# package tokens
packageclass.name = UglyForms
redirect.url	  = index.php?option=com_uglyforms&view=install
